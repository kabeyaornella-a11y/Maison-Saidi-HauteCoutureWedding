import type { CSSProperties,PropsWithChildren } from 'react';import styles from './Section.module.css';type Props=PropsWithChildren<{id?:string;className?:string;style?:CSSProperties;label?:string}>;export function Section({id,className='',style,label,children}:Props){return <section id={id} aria-label={label} className={`${styles.section} ${className}`} style={style}>{children}</section>}
